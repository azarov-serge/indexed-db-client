{
  "name": "@azarov-serge/indexed-db-client",
  "version": "1.0.2",
  "description": "A simple IndexedDB client for working with IndexedDB. Designed as ORM.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "directories": {
    "example": "example"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "format": "npx prettier \"src/**/*.{js,jsx,ts,tsx}\" --write",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build",
    "version:patch": "npm version patch && git push origin main && git push origin $(npm version --json | jq -r '.[]')",
    "version:minor": "npm version minor && git push origin main && git push origin $(npm version --json | jq -r '.[]')",
    "version:major": "npm version major && git push origin main && git push origin $(npm version --json | jq -r '.[]')",
    "publish:patch": "npm run version:patch && npm publish",
    "publish:minor": "npm run version:minor && npm publish",
    "publish:major": "npm run version:major && npm publish"
  },
  "keywords": [
    "indexeddb",
    "orm",
    "client",
    "database",
    "browser",
    "storage",
    "typescript"
  ],
  "author": "Sergey Azarov <your-email@example.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azarov-serge/indexed-db-client.git"
  },
  "bugs": {
    "url": "https://github.com/azarov-serge/indexed-db-client/issues"
  },
  "homepage": "https://github.com/azarov-serge/indexed-db-client#readme",
  "devDependencies": {
    "@typescript-eslint/parser": "7.2.0",
    "eslint": "8.57.0",
    "husky": "9.0.11",
    "lint-staged": "15.2.2",
    "prettier": "3.2.5",
    "typescript": "5.1.6"
  }
}
